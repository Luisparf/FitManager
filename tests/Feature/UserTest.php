<?php

namespace Tests\Feature;

use App\Models\Account;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Inertia\Testing\Assert;
use Tests\TestCase;

class ExampleTest extends TestCase
{
    /**
     * A basic test example.
     *
     * @return void
     */
    public function test_user()
    {

    // $regex = '/^[A-Za-zÀ-ú]{2,}(?:\s+[A-Za-zÀ-ú]{2,})+$/';
    // $name = 'John Doe';

    // // Test that the name matches the regex pattern
    // $this->assertMatchesRegularExpression($nregex, $name);

    // // Test that the name length is less than or equal to 100
    // $this->assertLessThanOrEqual(100, strlen($name));


    }
}
